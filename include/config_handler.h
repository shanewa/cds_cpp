#include <vector>
#include <string>
#include <stdlib.h>
using namespace std;

class config
{
    public:
        void read(vector<string> &);
        void load(vector<string> &);
    private:
};